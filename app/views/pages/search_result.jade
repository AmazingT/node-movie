extends ../layout

block content
  .container
    .row
      .panel.panel-default
        .panel-heading
          h3 搜索
            span(style="color:#1010ce;") “#{keyword}”
            | 的结果如下：
        .panel-body
          if movies && movies.length > 0
            each item in movies
             .col-md-4.col-sm-6
               .thumbnail
                 a(href="/movie/#{item._id}")
                  img(src="#{item.poster}", alt="#{item.title}")
                 .caption.text-center
                   h3 #{item.title}
                   p: a.btn.btn-primary(href="/movie/#{item._id}", role="button") 立即观看